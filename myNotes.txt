
npm i --save express mongoose morgan body-parser

npm i --save nodemon

// encrypt password
npm i --save bcrypt-nodejs

// jwt
npm i --save jwt-simple

// info available on jwt
jwt.io

// passport has helper functions to manage cookies, jwt
// has many helper functions. 
npm i --save passport passport-jwt

// salt + password = salt + hashpassword
// comparing passwords (signin)
// salt + hashpassword
// salt + submitted password = hash password.

// cors
npm i --save cors